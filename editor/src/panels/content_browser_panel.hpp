#pragma once

#include "ipanel.hpp"

namespace ignite {

    class ContentBrowserPanel : public IPanel
    {
    public:
        explicit ContentBrowserPanel(const char *windowTitle);

    private:

    };
}